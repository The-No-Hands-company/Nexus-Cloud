{
  "name": "nexus-cloud",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "dev:ecosystem": "bash ../scripts/ecosystem-local.sh start",
    "stop:ecosystem": "bash ../scripts/ecosystem-local.sh stop",
    "status:ecosystem": "bash ../scripts/ecosystem-local.sh status",
    "health:ecosystem": "bash ../scripts/ecosystem-local.sh health",
    "check": "tsc --noEmit",
    "test": "bun test src"
  },
  "devDependencies": {
    "bun-types": "^1.3.11",
    "typescript": "^5.8.3"
  }
}